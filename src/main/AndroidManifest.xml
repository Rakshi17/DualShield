<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dualshieldai">


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_placeholder"
        android:label="DualShield AI"
        android:roundIcon="@drawable/ic_logo_placeholder"
        android:supportsRtl="true"
        android:theme="@style/Theme.DualShieldAI"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.WorkerDashboardActivity" />
        <activity android:name=".ui.SupervisorDashboardActivity" />
        <activity android:name=".ui.WorkerDetailActivity" />
        <activity android:name=".ui.SensorDebugActivity" />

    </application>

</manifest>